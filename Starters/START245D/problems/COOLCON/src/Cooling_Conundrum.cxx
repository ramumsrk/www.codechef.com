#include <cstdlib>
#include <iostream>

using std::cin;
using std::cout;
using std::endl;

auto
main(void) -> int {
    int T {};
    cin>>T;
    while (T) {
        int X {}, Y {};
        cin>>X>>Y;
        T -= 1;
    }
    return EXIT_SUCCESS;
}