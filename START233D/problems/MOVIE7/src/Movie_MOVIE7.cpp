#include <cstdlib>
#include <iostream>

using std::cin;
using std::cout;
using std::endl;

auto
main(void) -> int {
    int T {};
    cin>>T;
    while (T) {
        int N {}, M {}, A {}, B {}, C {};
        cin>>N>>M>>A>>B>>C;
        T -= 1;
    }
    return EXIT_SUCCESS;
}